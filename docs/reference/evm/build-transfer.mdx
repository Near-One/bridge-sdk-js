---
title: buildTransfer
description: Build an unsigned transfer transaction for EVM chains
---

Builds an unsigned transaction for transferring tokens from an EVM chain.

## Signature

```typescript
builder.buildTransfer(validated: ValidatedTransfer): EvmUnsignedTransaction
```

## Parameters

<ResponseField name="validated" type="ValidatedTransfer" required>
  A validated transfer from `bridge.validateTransfer()`.
</ResponseField>

## Returns

```typescript
interface EvmUnsignedTransaction {
  to: `0x${string}`   // Bridge contract address
  data: `0x${string}` // Encoded function call
  value: bigint       // ETH value (for native or native fee)
  chainId: number     // Chain ID
}
```

## Examples

### Basic transfer

```typescript
const validated = await bridge.validateTransfer({
  token: "eth:0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48",
  amount: 1000000n,
  fee: 0n,
  nativeFee: 0n,
  sender: "eth:0x...",
  recipient: "near:alice.near",
})

const tx = evm.buildTransfer(validated)

// Use with viem
await walletClient.sendTransaction(tx)

// Use with ethers
await signer.sendTransaction(tx)
```

### Native ETH transfer

```typescript
const validated = await bridge.validateTransfer({
  token: "eth:0x0000000000000000000000000000000000000000",
  amount: parseEther("0.1"),
  fee: 0n,
  nativeFee: 0n,
  sender: "eth:0x...",
  recipient: "near:alice.near",
})

const tx = evm.buildTransfer(validated)
// tx.value includes the transfer amount
```

### With native fee

```typescript
const validated = await bridge.validateTransfer({
  token: "eth:0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48",
  amount: 1000000n,
  fee: 5000n,
  nativeFee: parseEther("0.001"),
  sender: "eth:0x...",
  recipient: "near:alice.near",
})

const tx = evm.buildTransfer(validated)
// tx.value includes the nativeFee
```

## Notes

- For ERC20 tokens, call `buildApproval()` first
- For native ETH, the `value` field includes the transfer amount plus any native fee
- For ERC20 tokens with native fee, the `value` field only includes the native fee

## Related

- [buildApproval](/reference/evm/build-approval)
- [validateTransfer](/reference/core/validate-transfer)
